/* eslint-env jest */

describe('DropTarget', () => {
  it('registers target with UPDATE_TARGET on mount');
  it('updates target with REMOVE_TARGET on change/interval');
  it('deregisters target with REMOVE_TARGET on unmount');
});
