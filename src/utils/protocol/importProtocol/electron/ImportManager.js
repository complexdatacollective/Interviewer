import { ImportManager } from "../shared/ImportManager";

export class ElectronImportManager extends ImportManager {
  importFromFile = async (callback) => { }
  importFromURI = async (uri, callback) => { }
}
