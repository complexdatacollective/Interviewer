/* eslint-env jest */

// import environments from '../../environments';
// import importer from '../importer';

describe('filesystem', () => {
  describe('Electron', () => {
    // const subject = importer(environments.ELECTRON);
    describe('ensurePathExists', () => {
      it('ensures path exists');
    });
    describe('copyFile', () => {
      it('copies a file');
    });
    describe('readFile', () => {
      it('reads a file');
    });
  });
});
